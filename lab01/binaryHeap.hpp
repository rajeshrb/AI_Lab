#include "UserState.hpp"

using namespace std;

class BinaryHeap
{
	vector<UserState*> elements;
	
	public:
		void Insert_Elements(UserState* elem)
		{
			int k=elements.size();
			while(1)
			{
				
};